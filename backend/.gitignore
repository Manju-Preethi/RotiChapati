.env
node_modules

# Ignore other image types if needed
# uploads/*.jpg
# uploads/**/*.jpg

# ALLOW PNG images in uploads (uncommented to include in git)
# Previously blocked lines are now commented out
# uploads/*.png
# uploads/**/*.png
